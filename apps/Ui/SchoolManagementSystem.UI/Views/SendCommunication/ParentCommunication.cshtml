
@model List<SchoolManagementSystem.Shared.ParentViewModel>
@{
    ViewData["Title"] = "Parent Communication";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<section id="page-title" class="padding-top-15 padding-bottom-15">
    <div class="row">
        <div class="col-sm-7">
            <a href="/SendCommunication/Index"> Index </a>/ Parent Communication
        </div>
    </div>
</section>

<div class="container-fluid container-fullw bg-white">
    <p class="text-small center text-danger">@ViewBag.ValidateErrorMessage</p>
    <div class="row">
        <div class="col-md-12">
            <h5 class="over-title margin-bottom-15">Parent <span class="text-bold">List</span></h5>
            <p>
                List of all parents.
            </p>
            <table class="table table-striped table-bordered table-hover table-full-width" id="sample_1">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>FullNames</th>
                        <th>
                            ID/Passport
                        </th>                     
                        <th class="hidden-xs">Phone</th>
                        <th class="hidden-xs">EmailAddress</th>
                        <th class="hidden-xs">
                            CreatedBy
                        </th>
                        <th class="hidden-xs">
                            CommunicationMethod
                        </th>
                        <th>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.ParentId</td>
                            <td>@item.Firstname  @item.Surname </td>
                            <td>@item.IdOrPassport</td>                           
                            <td class="hidden-xs">@item.CelPhoneNumber</td>
                            <td class="hidden-xs">@item.PersonalEmailAddress</td>
                            <td class="hidden-xs">@item.CreatedBy</td>
                            <td class="hidden-xs">@item.CommunicationMethod</td>
                            <td>
                                @Html.ActionLink("SendToParent", "SendToParent", new { id = item.ParentId })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>